apiVersion: v1
kind: Service
metadata:
  name: n8n-postgres-tailnet
  namespace: databases
  annotations:
    tailscale.com/hostname: "n8n-postgres"
    tailscale.com/tags: "tag:k8s-db"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    cnpg.io/cluster: n8n-postgres
    role: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
