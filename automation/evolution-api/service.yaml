apiVersion: v1
kind: Service
metadata:
  name: evolution-api
  namespace: automation
  labels:
    app.kubernetes.io/name: evolution-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evolution-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
