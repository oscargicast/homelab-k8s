apiVersion: v1
kind: Service
metadata:
  name: speedtest-tracker
  namespace: homelab
  labels:
    app.kubernetes.io/name: speedtest-tracker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: speedtest-tracker
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
