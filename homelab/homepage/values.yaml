replicaCount: 1

enableRbac: true

env:
  HOMEPAGE_ALLOWED_HOSTS: "*"

serviceAccount:
  create: true

resources:
  requests:
    memory: 64Mi
    cpu: 10m
  limits:
    memory: 128Mi
    cpu: 100m

config:
  kubernetes:
    mode: cluster
  bookmarks:
    - Observabilidad:
        - Grafana:
            - href: "https://grafana.oscargicast.com"
              description: Métricas y logs
        - Prometheus:
            - href: "http://prometheus.oscargicast.com:8080/"
              description: Prometheus UI
        - CNPG Dashboard:
            - href: "https://grafana.oscargicast.com/d/cloudnative-pg/cloudnativepg"
              description: PostgreSQL operator dashboard
  services:
    - Cluster:
        - Overview:
            description: Estado general del cluster
            href: "https://grafana.oscargicast.com"
            icon: kubernetes.png
            widget:
              type: prometheusmetric
              url: http://prometheus-kube-prometheus-prometheus.observability.svc.cluster.local:9090
              metrics:
                - label: Pods running
                  query: sum(kube_pod_status_phase{phase="Running"})
                - label: Pods pending
                  query: sum(kube_pod_status_phase{phase="Pending"})
                - label: Active alerts
                  query: sum(ALERTS{alertstate="firing"})
                - label: Ingress req/s
                  query: sum(rate(traefik_service_requests_total[5m]))
                  format:
                    type: number
                    suffix: /s
                - label: Node CPU %
                  query: 100 - (avg(rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)
                  format:
                    type: percent
                - label: Node mem used
                  query: node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes
                  format:
                    type: bytes
        - Argo CD:
            href: "https://argocd.oscargicast.com"
            description: GitOps controller
            icon: argo-cd.png
    - Automatización:
        - n8n:
            href: "https://n8n.oscargicast.com/"
            description: Workflows
            icon: n8n.png
    - Databases:
        - postgres-lab:
            description: CNPG cluster general purpose
            href: "https://grafana.oscargicast.com/d/cloudnative-pg/cloudnativepg?var-cluster=postgres-lab&var-namespace=databases"
            icon: postgres.png
            widget:
              type: prometheusmetric
              url: http://prometheus-kube-prometheus-prometheus.observability.svc.cluster.local:9090
              metrics:
                - label: Connections
                  query: sum(cnpg_backends_total{namespace="databases",pod=~"postgres-lab-.*"})
                - label: DB size
                  query: sum(cnpg_pg_database_size_bytes{namespace="databases",pod=~"postgres-lab-.*"})
                  format:
                    type: bytes
                - label: TPS
                  query: sum(rate(cnpg_pg_stat_database_xact_commit{namespace="databases",pod=~"postgres-lab-.*"}[5m])) + sum(rate(cnpg_pg_stat_database_xact_rollback{namespace="databases",pod=~"postgres-lab-.*"}[5m]))
                  format:
                    type: number
                    suffix: /s
        - n8n-postgres:
            description: CNPG cluster (backing n8n)
            href: "https://grafana.oscargicast.com/d/cloudnative-pg/cloudnativepg?var-cluster=n8n-postgres&var-namespace=databases"
            icon: postgres.png
            widget:
              type: prometheusmetric
              url: http://prometheus-kube-prometheus-prometheus.observability.svc.cluster.local:9090
              metrics:
                - label: Connections
                  query: sum(cnpg_backends_total{namespace="databases",pod=~"n8n-postgres-.*"})
                - label: DB size
                  query: sum(cnpg_pg_database_size_bytes{namespace="databases",pod=~"n8n-postgres-.*"})
                  format:
                    type: bytes
                - label: TPS
                  query: sum(rate(cnpg_pg_stat_database_xact_commit{namespace="databases",pod=~"n8n-postgres-.*"}[5m])) + sum(rate(cnpg_pg_stat_database_xact_rollback{namespace="databases",pod=~"n8n-postgres-.*"}[5m]))
                  format:
                    type: number
                    suffix: /s
  widgets:
    - resources:
        backend: kubernetes
        expanded: true
        cpu: true
        memory: true
        cputemp: false
        uptime: true
        disk: /
        label: System
    - search:
        provider: duckduckgo
        target: _blank
    - datetime:
        text_size: xl
        format:
          dateStyle: long
          timeStyle: short
          hour12: false
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
          showLabel: true
          label: "homelab"
        nodes:
          show: true
          cpu: true
          memory: true
          showLabel: true
